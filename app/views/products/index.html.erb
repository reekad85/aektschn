<p id="notice"><%= notice %></p>

<div class="container-fluid"> 
  <div class="header">
   <h1>Our Specialtea's</h1>
  </div>
  
  <div class="row">
  	<% cache(cache_key_for_products) do %>
    	<%= render @products %>
    <% end %>
  </div><!--/row-->
</div><!--/container-->

<% if signed_in? && current_user.admin? %>
	<span class="btn btn-default" role="button"><%= link_to 'New Product'.html_safe, new_product_path %></span>
<% end %>
